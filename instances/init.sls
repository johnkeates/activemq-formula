# activemq.instances.init

# Currently, this does not do much, as it only supports debian.
# Right now, you are better off using activemq.instances.debian directly and sets up everything at once

include:
  - activemq
  - activemq.instances.debian